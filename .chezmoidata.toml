# Valeurs par défaut (surchargées par ~/.config/chezmoi/chezmoi.toml [data])
profile = "desktop"  # desktop | headless
workDir = "/mnt/data/Work"
s3BucketBackup = "dhainach-backup"
s3BucketWork = "cd-work"
