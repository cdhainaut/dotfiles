{{- if ne .chezmoi.config.encryption "age" }}
# Sans encryption age configurée (CI, machine neuve), ignorer les fichiers chiffrés
.private_keys
.gitlab_wds_token
.aws
.cdsapirc
{{- end }}

{{- if eq .profile "headless" }}
# Headless: pas de GUI
.wezterm.lua
.local/share/plank
{{- end }}
